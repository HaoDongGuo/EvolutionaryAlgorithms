#统计rank0随着迭代的比例

#将剖分层次扩展到所有层次

#增强固定和拟合平面投影的鲁棒性

# 2016/7/28 Wednesday Night

1.观察rank0迭代比例
(基本上第三代以后就全部是rank0)√

2.改写剖分层次为整个种群
（原始代码是修改某一层的拥挤距离，暂时不用改）√

3.查找运行过程错误，try，catch，throw应用
（将代码易读，可维护，可扩展，方便实验）

- - 1.DT剖分的维度降了为啥剖分的时间没有明显的下降？
- -2.遍历单纯形的时间竟然是剖分时间的2倍！

--针对问题1，修改维度后4目标有bug！

--针对问题2，分析遍历时间的花费；

--加入Clustered NSGAII的代码功能；
